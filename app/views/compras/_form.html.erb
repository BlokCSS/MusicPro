<%= form_with(model: compra, class: "box") do |form| %>
  <% if compra.errors.any? %>
    <div class="notification is-danger">
      <h2><%= pluralize(compra.errors.count, "error") %> prohibited this compra from being saved:</h2>

      <ul>
        <% compra.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre, class: "label" %>
    <div class="control">
      <%= form.text_field :nombre, class: "input" %>
    </div>
  </div>

  <div class="field">
    <%= form.label :total, class: "label" %>
    <div class="control">
      <%= form.text_field :total, class: "input" %>
    </div>
  </div>

  <div class="field">
    <div class="control">
      <%= form.submit 'Guardar', class: "button is-primary" %>
    </div>
  </div>
<% end %>
